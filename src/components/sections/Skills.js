import { Section } from '@components/sections/components/Section'
import { TextP } from '@components/index'
import React from 'react'

export const Skills = ({ data }) => {
  return <Section title={data?.title}></Section>
}
